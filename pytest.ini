[pytest]
minversion = 6.0
addopts =
    --strict-markers
    --tb=short
    --disable-warnings
    --cov=app/utils
    --cov-report=term-missing
    --cov-fail-under=85
testpaths = tests
python_files = test_*.py
markers =
    slow: teste lente
    integration: teste de integrare
